{
  "framework": "vite",
  "buildCommand": "git lfs pull && npm install --force && npm run build",
  "installCommand": "echo done",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
